# 飞书签名生成器 · 配置教程

一个跑在**飞书内部（妙笔 / Magic Builder）**的网页工具：在工具里挑好内容 → 复制一条链接 → 粘进**飞书「个人主页 → 个性签名」栏**，飞书会把它渲染成一条**蓝色可点的签名**。签名始终只有一条链接和一个图标，点击后跳到设定的文档。

两个主模式：

- **入职时长**：选入职日期，用「等级 / 日月星 / 剩余天数 / 下一等级」组件组合文案，再选图标。组件会随时间自动更新（例：`Lv. 16 ☀️🌙⭐（还需要 9 天升级至 Lv.17）`）。
- **自定义文字**：写任意文字 + 任意跳转链接 + 选图标；**文字也可留空，只复制一个图标**。

两个主模式都可以开启**周四尾缀**：周四在原签名后追加一段自定义文字，其他日期仍只显示原签名。前端默认换行，也可改为空格、符号或任意自定义连接内容。

本教程教你从零搭一套自己的（你需要妙笔 Token + 一个跳转目标文档）。

---

## 0. 原理（先看一眼，少踩很多坑）

- 老方案 `cgth.ink/?t2=...&u=...` 依赖第三方的飞书链接预览机器人，**已失效**。
- 新方案用**妙笔自己的链接预览**：签名链接形如
  `https://magic.solutionsuite.cn/r?fid={云函数id}&date=...&k=...&u={点击跳转目标}`。
  把它粘进个性签名栏，飞书侧由妙笔的预览机器人把它渲染成蓝色链接文字；主模式还会带前缀图标。人点击时 307 跳到 `u`。
- 内容由一个**妙笔云函数（FaaS）实时决定**：入职时长模式在每月入职纪念日升级，并把计算结果填入文案组件；自定义模式直接返回文字；开启周四尾缀时，FaaS 再按北京时间决定是否追加。

### 五个关键机制（FaaS / 前端已处理好，理解即可）

1. **预览是 POST，不是 GET**：妙笔 `/r?fid=` 的预览**不是** GET 转发 query，而是飞书以 **POST + `url.preview.get` 事件**调用云函数，用户粘贴的原始链接在请求体的 **`event.context.url`** 里。云函数只读 GET query 会拿到空值（永远一颗 ⭐）——必须解析 POST body。
2. **始终恰好一个图标（互斥）**：inline 的 `image_key = 你选的图标 || defaultLinkIcon`。没选图标时用飞书之父的默认链接符号 `img_v3_02bj_a88d6829-365b-4bec-a574-5733ba95cc7g`。周四尾缀与原签名共用同一条链接和同一个图标，不会产生额外的链接符号。
3. **展示与跳转分层**：FaaS 只返回展示数据（标题，以及主模式的图标），不返回 `inline.url`；点击跳转由外层妙笔 `/r?...&u=...` 路由执行 307。把目标地址写进 `inline.url` 会让飞书再次打开签名时改用目标链接标题，覆盖太阳/月亮动态文案。
4. **周四尾缀与主签名合并**：同一条链接使用 `thursday_text` 和 `thursday_sep`。周四返回「原标题 + 连接符 + 尾缀」，其他日期原样返回主标题。`thursday_sep` 可以是空格、符号、换行或空串。
5. **沉浸式头像/名字**：工具跑在飞书内部时，`window.magic.getCurrentUserInfo()` 能**零授权**拿到当前访问者的真实头像和名字（`{name, avatar_url}`），预览就是「你本人」。**这是部署在妙笔而非 Vercel 的主因**——普通浏览器没有 `window.magic`，拿不到（自动回退占位）。

整条链路：
```
签名链接 /r?fid=X&date=Y&k=...&thursday_text=...&thursday_sep=...&u=DOC
   ├─ 飞书取预览：POST 事件 → 妙笔 → 调云函数 X（body.event.context.url 带全部参数）→ 返回 title（主模式另带 image_key）
   └─ 人点击：307 → 跳转到 DOC
```

> **为什么没有「拼字」模式**：飞书之父的拼字是把一张图切成 5×5=25 块、每块一条预览链接、25 条用空格拼起来粘进签名。一条签名 25~75 个链接太长，已弃用。带 `withNewLine` 的大图分类（大狗头/奥特曼/黑神话）因此**从图标选择器里排除**。

---

## 1. 准备

### 1.1 安装妙笔技能
解压 `magic-builder.skill.zip`，把里面**每个子技能目录** + `_shared` 平铺到 skills 根目录（如 `~/.claude/skills/`，注意平铺、不要带 `magic-builder/` 父层）。装完应有 `publish-magic-page`、`publish-magic-faas`。

### 1.2 配置妙笔 Token
```bash
printf '你的妙笔Token' > ~/.magic-token   # 读取优先级：MAGIC_TOKEN 环境变量 → ~/.magic-token → 项目根 .magic-token
chmod 600 ~/.magic-token
```

### 1.3 Node.js 18+
```bash
node --version
```

---

## 2. 第一步：部署动态预览云函数（FaaS）

源码见 [`faas/signature-preview.js`](faas/signature-preview.js)（逻辑已写好，一般无需改）。发布：
```bash
bunx magic-builder@1.3.0 faas publish ./faas/signature-preview.js \
  --name join_tenure_signature_preview --format json
```
CLI 会自动读取 `~/.magic-token`，无需把 Token 放进命令行。记下输出的 **fid**（形如 `vlojAfVNVfQ`）和 **record_id**（形如 `recvlojAfVNVfQ`）。之后改函数重发时加 `--id <record_id>` 更新同一条记录，fid 不变。

### 云函数接受的参数

| 参数 | 用途 |
|---|---|
| `date` | 入职日期 `YYYY-MM-DD`（入职时长模式） |
| `template` | 旧链接兼容参数：`symbols` / `level` / `countdown` / `fullDay` / `fullMonth`；新前端不再生成 |
| `format` | 当前前端使用的可组合文案，支持 `{等级}` / `{日月星}` / `{剩余天数}` / `{下一等级}` |
| `prefix` | 旧链接兼容参数：前缀文字，如 `Lv. ` |
| `k` | 飞书图标 `image_key`（可空，空则用 defaultLinkIcon） |
| `text` | 自定义文字模式：直接作为签名标题 |
| `thursday_text` | 可选的周四尾缀；存在且非空时启用 |
| `thursday_sep` | 原标题与尾缀间的连接内容；缺失默认一个空格，显式空串表示直接相连，`%0A` 表示换行 |
| `weekday` | 旧版独立周四链接的兼容参数；新前端不再生成 |
| `pv` | 前端预览协议版本，用于更换 URL 并隔离飞书旧缓存；FaaS 业务逻辑不读取它 |
| `desc` | 自定义文字的摘要（可空，仅聊天卡片场景显示） |
| `u` | 点击签名后的跳转目标，由妙笔 `/r` 路由执行 307 |
| `raw` | 图标-only：`raw=1` 时 title=`t`（换行或零宽空格），只显示图标 |
| `t` | 配合 `raw`，`%0A`=换行，空=零宽空格 |

### 验证（命令行）
```bash
# 入职时长（GET 直连）
curl "https://magic.solutionsuite.cn/api/faas/YOUR_FID?date=2025-04-28&template=countdown&prefix=Lv.%20"
# → 当前等级、日月星和剩余天数会随日期自动变化

# 模拟飞书预览的 POST 路径（参数在 body 的事件 URL 里）
curl -X POST -H "Content-Type: application/json" \
  -d '{"schema":"2.0","event":{"context":{"url":"https://magic.solutionsuite.cn/r?fid=YOUR_FID&date=2025-04-28&template=countdown&prefix=Lv.%20"}}}' \
  "https://magic.solutionsuite.cn/api/faas/YOUR_FID"
# 两者标题应一致 = body 解析正常

# 周四尾缀（真实显示结果取决于当前北京时间是否为周四）
curl "https://magic.solutionsuite.cn/api/faas/YOUR_FID?text=%E4%B8%BB%E7%AD%BE%E5%90%8D&thursday_text=%F0%9F%94%A5%20CrazyThursdayVme50%20%F0%9F%94%A5&thursday_sep=%0A"
# 周四 → 返回「主签名\n🔥 CrazyThursdayVme50 🔥」；其他日期 → 只返回「主签名」；图标始终不变
```

---

## 3. 第二步：配置生成器前端

前端就是 [`index.html`](index.html)：单文件、自包含（图标库 2280 个 + 封面图都内联），跑在飞书 HTML Box 沙箱里。你只需改 `<script>` 顶部四个常量：
```js
const PREVIEW_FID = 'YOUR_FID';                                  // ← 第一步拿到的云函数 id
const PREVIEW_SCHEMA = '5';                                      // 协议变更时递增，隔离飞书旧缓存
const DEFAULT_TARGET = 'https://你的飞书文档链接';                 // 留空跳转链接时的默认目标
const DEFAULT_LINK_ICON = 'img_v3_02bj_a88d6829-365b-4bec-a574-5733ba95cc7g'; // 没选图标时的链接符号（一般不改）
```

链接怎么拼（`buildUrl`，已写好）：
```js
// 周四尾缀与主签名共用同一条链接；普通链接和 raw 图标链接都调用它
function appendThursdayParams(params) {
  if (!thursdayEnabled) return;
  params.set('thursday_text', thursdayText.trim());
  params.set('thursday_sep', thursdaySeparator); // 空格、符号、\n 或任意自定义内容
}

// 自定义模式：只选图标、不写字 → raw 链接（签名里只显示一个图标）
if (mode === 'custom' && !customText.trim() && selectedKey) {
  const raw = new URLSearchParams({
    fid: PREVIEW_FID, pv: PREVIEW_SCHEMA, raw: '1', k: selectedKey, t: '', u: link
  });
  appendThursdayParams(raw);
  return `${MAGIC_BASE}/r?${raw}`;
}
// 否则：入职时长 / 自定义文字
const p = new URLSearchParams({ fid: PREVIEW_FID, pv: PREVIEW_SCHEMA });
if (mode === 'custom') p.set('text', customText);
else { p.set('date', date); p.set('format', format); }
if (selectedKey) p.set('k', selectedKey);
p.set('u', link);
appendThursdayParams(p);
return `${MAGIC_BASE}/r?${p}`;
```

改前端时要保持的几点约束（妙笔沙箱）：
- `<head>` 的 `use-iframe` + `html-box-height-mode=auto` meta 不能删。
- **禁止外链 JS（esm.sh 等）/ `localStorage`**（沙箱 CSP 会拦）；图标数据 `PRESETS` 和封面（`data:image/jpeg;base64,…`）都必须内联，不能引用同级文件。
- 沉浸头像靠 `window.magic.getCurrentUserInfo()`，已用 `if (!window.magic) return` 兜底，非飞书环境自动回退占位。

---

## 4. 第三步：发布到妙笔

```bash
bunx magic-builder@1.3.0 page publish ./index.html \
  --title '飞书签名生成器' --format json
```
输出的 **Feishu Tab/Sidebar** 那两条 applink 是**在飞书客户端里打开**的入口（沉浸头像只在这里生效）。发布映射写进 `.magic-apps.json`；更新时传 `--id <app id>`，即可覆盖同一个 app。

---

## 5. 使用与验证

> 沉浸头像/名字 **只在飞书客户端内打开**才有（`window.magic` 注入）；浏览器直接开独立页 → 显示占位「你的名字」。

1. 在飞书里通过 Tab/Sidebar applink 打开工具。
2. 切「入职时长 / 自定义文字」并设置主签名。入职时长默认带完整升级文案，可直接增删动态组件或穿插文字；需要时再开启「周四尾缀」。
3. 点「复制签名链接」。
4. 粘进飞书「个人主页 → 个性签名」栏。周四会在原签名后追加尾缀，其他日期主签名不变。

---

## 6. FAQ / 排查

**Q: 签名只显示一颗 ⭐（没拿到 date）。**
A: 把云函数临时改成回显 body，看飞书到底传了什么：
```js
module.exports = async (request) => {
  let body=""; try{ body=await request.text(); }catch(e){}
  return new Response(JSON.stringify({inline:{i18n_title:{zh_cn:"BODY["+body.slice(0,150)+"]"}},expire_strategy:"60s"}),
    {headers:{"Content-Type":"application/json; charset=utf-8"}});
};
```
重发后粘一条**没粘过的**链接（加 `&_cb=随机` 避开缓存），看 body 里用户 URL 在哪个字段，调整取值。排查完换回正式版。

**Q: 改了内容但飞书不更新。**
A: 飞书按 URL 缓存预览（`expire_strategy:"1day"`，东八区自然日）。要立刻看新效果就换 URL（改任一参数或加 `&_cb=`）。

**Q: 换行或自定义连接符如何传给 FaaS？**
A: 前端用 `URLSearchParams` 直接写入 `thursday_sep`；换行会编码为 `%0A`，显式空串会保留为“直接相连”。FaaS 不再额外补空格。

**Q: 图标看不见。**
A: 预览里小图标染了蓝色滤镜（原图是白色，不染看不见）。`k` 传的是飞书 `image_key`，确认 key 有效、预览环境能访问 `open.feishu.cn` 图片域。

**Q: 链接太长？**
A: 默认完整文案通常约 200–300 字符；调整后的链接长度会随文案增加。粘进飞书后飞书通常自动包成 `applink.larkoffice.com/client/message/link/open?token=…` 短链——那是飞书侧自动产生的，无需也无法手动生成。

---

## 7. 自定义

- **等级规则**：入职当天是 Lv.1，此后每到月度入职纪念日升级；29/30/31 日在短月份按月末升级，下个月恢复原始入职日。
- **符号规则**：`faas/signature-preview.js` 的 `symbols()`（每 36/12/3/1 级 = 👑☀️🌙⭐）。前端预览复用同一等级结果。
- **文案组件**：`format` 支持 `{等级}`、`{日月星}`、`{剩余天数}`、`{下一等级}`；只做固定文本替换，不执行表达式。
- **默认跳转**：前端 `DEFAULT_TARGET`。
- **周四尾缀**：前端在主链接上增加 `thursday_text` 和 `thursday_sep`。FaaS 用 UTC+8 判断星期，只在周四追加，并保留原签名的 `image_key`。
- **图标库**：前端内联的 `PRESETS`（2280 个，提取自飞书「创意个性签名」文档的图标组件；含 `withNewLine` 的大图分类已排除）。重新提取见仓库提交记录。
- **封面图**：`.profile-cover` 背景的 `data:image/jpeg;base64,…`（替换前先压到 ~60KB 再转 base64，否则 HTML 过大）。

---

### 组件清单（本仓库这套实例）

| 组件 | 位置 / ID |
|---|---|
| 生成器前端 | `index.html`（自包含）→ app `vlo9H4XAHeM` |
| 动态预览云函数 | `faas/signature-preview.js` → fid `vlojAfVNVfQ` |
| 封面源图 | `image.png`（已 base64 内联进 index.html，部署不依赖它） |
| 妙笔域名 | `https://magic.solutionsuite.cn` |

> 别人复刻时：fid、DEFAULT_TARGET、app id 都是你自己的，按上面步骤替换即可。
