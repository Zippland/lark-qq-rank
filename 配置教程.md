# 飞书入职时长签名生成器 · 配置教程

一个网页工具：复制一条链接 → 粘到飞书个性签名/聊天里，飞书会渲染成一张可点击的预览卡片。支持两种模式：

- **入职时长**：选入职日期/样式/图标，卡片显示**会随时间自动更新**的入职时长（例：`Lv. 14 ☀️⭐⭐ (还需要 20 天升级至 Lv.15)`）。
- **自定义文字**：输入任意文字 + 任意跳转链接（文字-链接自定义），卡片显示你写的文字，点击跳到你填的链接。

本教程教你从零搭一套自己的（你需要自己的妙笔 Token 和一个跳转目标文档）。

---

## 0. 原理（为什么这么做，先看一眼能少踩很多坑）

- 老方案 `cgth.ink/?t2=...&u=...` 依赖第三方的飞书链接预览机器人，**已失效**。
- 新方案用**妙笔（Magic Builder）自己的链接预览**：链接形如
  `https://magic.solutionsuite.cn/r?fid={云函数id}&date=...&template=...&prefix=...&k=...&u={点击跳转目标}`。
  飞书侧由妙笔的预览机器人渲染卡片，人点击时 307 跳到 `u`。
- 卡片内容由一个**妙笔云函数（FaaS）实时计算**：每次飞书来取预览，函数用「今天 − 入职日期」算出当前 Lv. 和符号，所以**会自己长**，不用手动更新。

> **核心坑（本教程的 FaaS 已处理好）**：妙笔 `/r?fid=` 的预览**不是** GET 转发 query，而是飞书以
> **POST + `url.preview.get` 事件**调用云函数，用户粘贴的原始链接在请求体的 **`event.context.url`** 里。
> 如果云函数只读 GET query，会拿到空值、永远只显示一颗 ⭐。必须解析 POST body 才能拿到 `date` 等参数。

整条链路：
```
签名链接 /r?fid=X&date=Y&...&u=DOC
   ├─ 飞书取预览：POST 事件 → 妙笔 → 调云函数 X（body.event.context.url 带全部参数）→ 实时算 → 返回卡片标题
   └─ 人点击：307 → 跳转到 DOC
```

---

## 1. 准备

### 1.1 安装妙笔技能
下载并解压 `magic-builder.skill.zip`，把里面**每个子技能目录** + `_shared` 平铺到你的 skills 根目录
（如 `~/.claude/skills/`，注意是平铺，不要带 `magic-builder/` 父层，否则 `../_shared/` 引用会断）。
安装后应有 `publish-magic-page`、`publish-magic-faas` 等目录。

### 1.2 配置妙笔 Token
把你的妙笔开发 Token 写进 `~/.magic-token`（脚本读取优先级：环境变量 `MAGIC_TOKEN` → `~/.magic-token` → 项目根 `.magic-token`）：
```bash
printf '你的妙笔Token' > ~/.magic-token
```

### 1.3 需要 Node.js 18+
```bash
node --version
```

---

## 2. 第一步：部署动态预览云函数（FaaS）

云函数源码见本仓库 [`faas/signature-preview.js`](faas/signature-preview.js)（逻辑已写好，一般无需改）。
发布：
```bash
node ~/.claude/skills/publish-magic-faas/scripts/publish.mjs \
  --token "$(cat ~/.magic-token)" \
  --file ./faas/signature-preview.js \
  --name "join_tenure_signature_preview"
```
成功输出里记下 **`id`**（形如 `vlojAfVNVfQ`），这就是你的 **fid**，下一步要填。

> 之后修改云函数重新发布，加 `--id rec<上面的id>` 即可更新同一条记录，fid 不变。

### 验证云函数（命令行，GET 直连）
```bash
# 把 YOUR_FID 换成上一步的 id
curl "https://magic.solutionsuite.cn/api/faas/YOUR_FID?date=2025-04-28&template=fullMonth&prefix=Lv.%20"
# 期望返回类似：{"inline":{"i18n_title":{"zh_cn":"Lv. 14 ☀️⭐⭐ (还需要 20 天升级至 Lv.15)"}},...}
```
也可模拟飞书预览的 POST 路径（参数走 body 里的事件 URL）：
```bash
curl -X POST -H "Content-Type: application/json" \
  -d '{"schema":"2.0","event":{"context":{"url":"https://magic.solutionsuite.cn/r?fid=YOUR_FID&date=2025-04-28&template=fullMonth&prefix=Lv.%20"}}}' \
  "https://magic.solutionsuite.cn/api/faas/YOUR_FID"
# 两者标题应一致，说明 body 解析正常
```

---

## 3. 第二步：配置生成器前端

前端就是本仓库的 [`index.html`](index.html)（单文件，已内联 2280 个图标库，无第三方依赖，含「入职时长 / 自定义文字」双模式切换）。
你只需改 `<script>` 里两个常量：

```js
const DEFAULT_TARGET = 'https://你的飞书文档链接';  // 留空跳转链接时的默认目标（换成你自己的）
const MAGIC_BASE = 'https://magic.solutionsuite.cn';
const PREVIEW_FID = 'YOUR_FID';                    // ← 换成第一步拿到的云函数 id
```

链接是怎么拼出来的（`buildUrl`，已写好，理解即可，按模式分支）：
```js
const params = new URLSearchParams();
params.set('fid', PREVIEW_FID);
if (mode === 'custom') {
  params.set('text', customText);              // 自定义文字模式：任意文字
} else {
  params.set('date', date);                    // 入职时长模式
  params.set('template', template);
  if (prefix) params.set('prefix', prefix);
}
if (selectedKey) params.set('k', selectedKey); // 图标（两模式通用）
// 自定义模式可填自己的跳转链接，留空回退 DEFAULT_TARGET
const link = (mode === 'custom' && customLink.trim()) || DEFAULT_TARGET;
params.set('u', link);
return `${MAGIC_BASE}/r?${params.toString()}`;
```

> 云函数侧：`text` 参数存在就走自定义文字（静态返回该文字，可带 `desc` 摘要）；否则按 `date` 算入职时长。详见 [`faas/signature-preview.js`](faas/signature-preview.js)。

> 前端跑在飞书文档内的 HTML Box 沙箱里，`<head>` 已声明 `use-iframe` 与 `html-box-height-mode=auto`，
> 且**禁止 esm.sh 等外链 / `localStorage`**（沙箱 CSP 会拦），图标数据已内联。改前端时保持这些约束。

---

## 4. 第三步：发布生成器页面

```bash
node ~/.claude/skills/publish-magic-page/scripts/publish.js publish index.html --title "飞书签名生成器"
```
输出里的 **Independent Page**（`https://magic.solutionsuite.cn/html-box/xxxx`）就是工具地址，
**Feishu Tab/Sidebar** 那两条 applink 可直接在飞书里打开。
（发布映射会写进 `.magic-apps.json`，对同一文件再发布即「更新」同一个 app。）

---

## 5. 使用与验证

1. 打开生成器页面，顶部切「入职时长 / 自定义文字」模式：
   - 入职时长：选日期 / 样式 / 前缀 / 图标。
   - 自定义文字：写文字 + 填跳转链接（留空用默认）+ 选图标。
   然后点「复制签名」。
2. 把复制到的链接粘到飞书任意聊天框或你的个性签名栏。
3. 应渲染出卡片，标题是当前入职时长（不是单个 ⭐）。点击跳到你设的 `TARGET_URL`。

- ✅ 显示符号串（如 `Lv. 14 ☀️⭐⭐ ...`）= 成功。
- ❌ 只显示一颗 `⭐` = 云函数没拿到 `date`。多半是云函数没解析 POST body，或妙笔放 URL 的字段变了——
  本仓库 FaaS 已含「`event.context.url` + 递归兜底」两层，正常不会发生；若发生，按下方 FAQ 抓 body 排查。

---

## 6. FAQ / 排查

**Q: 卡片能渲染但只有一颗星。**
A: 云函数 `date` 为空。把云函数临时改成回显 body 看飞书到底传了什么：
```js
module.exports = async function (request) {
  let body = ""; try { body = await request.text(); } catch (e) {}
  return new Response(JSON.stringify({
    inline: { i18n_title: { zh_cn: "BODY[" + body.slice(0,150) + "]" } },
    expire_strategy: "60s"
  }), { headers: { "Content-Type": "application/json; charset=utf-8" } });
};
```
重发后换一个**没粘过的**链接（加个 `&_cb=随机`，避开飞书缓存）粘进飞书，看标题里的 body，
确认用户 URL 在哪个字段，调整 FaaS 取值路径。排查完记得换回正式版。

**Q: 改了内容但飞书不更新。**
A: 飞书按 URL 缓存预览，`expire_strategy:"1day"`（东八区自然日）。要立刻看新效果就换 URL（改任一参数或加 `&_cb=`）。

**Q: 入职时长每天会自己变吗？**
A: 会。函数每次预览都按「今天」算，`1day` 缓存保证每个自然日刷新一次。

**Q: 图标不显示？**
A: `k` 传的是飞书 `image_key`，函数把它作为 `inline.image_key` 返回。确认 key 有效、且预览环境能访问 `open.feishu.cn` 图片域。

**Q: 链接太长？**
A: `/r?fid=...` 约 180–220 字符（比老 cgth.ink 的 1000+ 短得多）。粘进飞书后，飞书通常会再自动包成
`applink.larkoffice.com/client/message/link/open?token=...` 短链——那是飞书侧自动产生的，无需也无法手动生成。

---

## 7. 自定义

- **改符号规则**：编辑 `faas/signature-preview.js` 的 `symbols()`（每 36/12/3/1 月对应 👑☀️🌙⭐）。
- **改文案/样式**：`template` 的 `symbols|fullDay|fullMonth` 三个分支。前端 `index.html` 的 `renderPreview()`
  要与 FaaS 算法保持一致，预览面板才能所见即所得。
- **改默认跳转**：前端 `DEFAULT_TARGET`（自定义模式留空跳转链接时用它）。
- **改图标库**：前端内联的 `__PRESETS__`（2280 个，提取自飞书「创意个性签名」文档的图标组件；如需重新提取见仓库提交记录）。
- **自定义文字模式**：前端 `mode` 切换；云函数靠 `text`/`desc` 参数返回静态预览。

---

### 组件清单（本仓库这套实例）

| 组件 | 位置 / ID |
|---|---|
| 生成器前端 | `index.html` → app `vlo9H4XAHeM` |
| 动态预览云函数 | `faas/signature-preview.js` → fid `vlojAfVNVfQ` |
| 妙笔域名 | `https://magic.solutionsuite.cn` |

> 别人复刻时：fid、DEFAULT_TARGET、app id 都会是你自己的，按上面步骤替换即可。
